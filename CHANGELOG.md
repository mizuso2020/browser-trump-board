# 更新履歴（詳細版）— ブラウザ・トランプ＆ボード

チャットでの依頼・修正を時系列で記録。Webサイト・ゲーム・インフラの変更を含む。

---

# 2026-07-13 — 人狼「各自のスマホ」解禁・同期バグ修正（最大の手間）

## 新機能：人狼を各人のスマホで遊べるようにした

| 項目 | 内容 |
|------|------|
| モード | **各自のスマホ**（ルームモード） |
| 参加方法 | ホストがルーム作成 → **QRコード / 参加リンク** を共有 → 参加者は名前だけ入力 |
| 本番URL | https://browser-trump-board.com/games/play.html?game=werewolf |
| 参加URL例 | `play.html?game=werewolf&mode=room&code=XXXX` |
| サーバー | `party-games-room`（port 8765）、nginx `/games/api/` 経由 |
| 主な変更ファイル | `room.js`, `sync.js`, `play.js`, `utils.js`, `room_server.py`, `werewolf.js`, `secrets.js` |

### 実装の要点

- ルームAPIで参加者を同期（ポーリング 400ms、`Cache-Control: no-store`）
- 参加時に `player` パラメータ付きURLでプレイヤーIDを保持（localStorage / sessionStorage / Cookie）
- 役職など秘密情報は `hostSecrets` / `playerSecrets` に分割保存
- ホストはロビーで構成選択 → 「人狼を始める」で全員へ `wolf_ready`（役職確認）へ

### ついでに調整したUX

- 議論時間 **5分 → 3分**
- ゲーム終了時の **役職一覧を参加者全員が閲覧可能**（`revealedRoles` を公開データに保存）

### その後の追記（同日）

- **ノーマル○×・消える○×・マトリョーシカ○×** を各自のスマホ（ルームモード）で解禁
  - 2人専用、自分の番だけ操作（`isMyTurn`）
  - ホスト＝先攻（〇/赤）、2人目＝後攻（×/青）
- ゲーム終了後に **「トップへ」** と **その人数で遊べるゲーム一覧** を表示（`renderEndGameNav`）

---

## てこずったバグ3件（原因と対策まとめ）

今回いちばん時間がかかったのは **「ゲーム開始後、参加者1人だけロビーに残る」** 問題。  
加えて対策の副作用と、人狼役だけのクラッシュが重なった。

### 1. 一人だけロビーに残る（メインのバグ）

| | 内容 |
|--|------|
| **症状** | ホストが「人狼を始める」後、参加者のうち **ランダムに1人だけ** ロビー待機のまま。ブラウザ・参加順・待ち時間は無関係。強制再読み込みでも直らないことがある |
| **原因** | ホストがゲーム開始を保存した **後** に、遅れて届いた **古いロビーデータの PUT** がサーバーを上書きし、状態が `wolf_ready` → `lobby` に戻っていた |
| **なぜホストだけ進めるか** | ホストは開始ボタンで **ローカル画面を即切替**。参加者は **サーバーの状態をポーリング** して切替えるため、上書きのタイミングで取り残される |
| **対策** | `room_server.py` で **ゲーム開始後はロビーへ戻す保存を拒否**（`reject_public_put`）。クライアント側は同期の直列化・`gameStartedAt`・取り残し時の自動リカバリ（`hardRecoverToRoom`） |

### 2. 「保存が古くなりました」でルームが作れない（対策の副作用）

| | 内容 |
|--|------|
| **症状** | 「ルームを作る」直後に `保存が古くなりました。画面を更新してください` で落ちる |
| **原因** | 上記対策の「古い保存拒否」が **ロビー作成直後の正常な保存** までブロック。スマホとサーバーの **時刻ずれ** で `updatedAt` が古いと判定された |
| **対策** | **ロビー中は厳しいチェックをしない**。**ゲーム開始後だけ** 古い保存・フェーズ逆行を拒否。クライアントは `updatedAt` を `max(前回+1, Date.now())` で送信 |

### 3. 人狼を始めると一人だけエラー（`cgq58v3u` 等のプレイヤーID）

| | 内容 |
|--|------|
| **症状** | `Cannot read properties of undefined (reading 'プレイヤーID')` でエラー画面 |
| **原因** | オンラインでは全員の役職一覧（`roles`）は公開しない。**人狼役の参加者** だけ、仲間表示で存在しない `roles` オブジェクトを参照してクラッシュ |
| **なぜホストは大丈夫か** | ホストは `hostSecrets.roles` で全員分を持っている。参加者の人狼だけ `roles` が空 |
| **対策** | `getRoles` の安全化、人狼の仲間は `playerSecrets.wolfMates` から表示、`roleConfirmed` の未定義ガード |

---

## 変更ファイル（2026-07-13）

| ファイル | 変更概要 |
|----------|----------|
| `api/room_server.py` | `merge_players`, `resolve-player`, `reject_public_put`, `gameStartedAt`, stale/started 拒否 |
| `js/room.js` | 同期直列化、ID復元、ロビー待機UI、自動リカバリ、ホスト開始前の再取得 |
| `js/sync.js` | `cache: no-store`, ポーリング短縮, `resolvePlayer`, `updatedAt` 補正 |
| `js/utils.js` | `getStablePlayerId`, `buildRoomUrl`, `rememberRoomSession` |
| `js/play.js` | 参加後 `player` 付きURLへ遷移 |
| `js/games/werewolf.js` | 非ホストのロビー破壊防止、人狼仲間表示、議論3分、終了時 `revealedRoles` |
| `js/games/tic-tac-toe.js` / `vanishing-ttt.js` / `matryoshka-ttt.js` | 各自スマホ向け `isMyTurn`、番表示 |
| `js/secrets.js` | 終了時 `revealedRoles` を公開側に残す |
| `js/game-registry.js` | ○×3種のルームモード解禁 |
| `css/style.css` | ゲーム終了ナビ |
| `room.html` / `play.html` | キャッシュバスト |

---

## Version 2（予定）

| 項目 | 内容 |
|------|------|
| 独自ドメイン | 取得 + Let's Encrypt（年1,000〜2,000円程度）— **browser-trump-board.com 運用中** |
| 各自のスマホ | ルームコード / QR で参加、端末ごとに操作 — **人狼・○×3種は 2026-07-13 解禁** |
| オンライン | Firebase 本番設定、ルームAPI 安定運用 |
| 公開拡大 | コード済みゲーム（大富豪・ポーカー系など）の順次公開 |

**V1（v1.0.0）は 2026-07-12 固定。** 以降の大きな変更は V2。

---

# 2026-07-12（午前）— V1確定・ローカル起動・仕上げ

## サイト運用
- 累計プレイ回数が **200回** のまま表示される不具合を修正（実際は約243回）
- `stats-boot.js` を 243 に更新、EC2 に反映
- ローカル起動時も EC2 API から正しい回数を取得するよう `stats.js` 改修
- **V1はこれで固定** と決定
- **V2は独自ドメイン + 各自スマホ** で進める方針を文書化

## ローカル起動（ZIP用）— 複数回修正
- `index.html` 直開きで CSS/JS が読めず **ボタンが効かない** 問題を調査
- 原因: ZIP内から開く / `file://` / Temp フォルダ展開
- `アプリを開く.bat` を新設（当初 Python 依存 → PCに Python なしで失敗）
- `scripts/local-server.ps1` を新設（PowerShell のみ、Python不要）
- ポート **8765** が Windows 側で使用中 → **29500** に変更
- 29500 も塞がる場合は **29501 等へ自動切替**
- `index.html` に `file://` 直開き警告オーバーレイ
- `js/file-protocol-guard.js` を `room.html` / `play.html` にも追加
- `起動のしかた.txt` を全面改訂（Python手順削除、bat 必須を明記）
- ユーザー展開フォルダへ修正ファイルをコピー

## GitHub / Version 1 アーカイブ（同日〜前日）
- GitHub リポジトリ `mizuso2020/browser-trump-board` 作成
- タグ `v1.0.0` 付与
- `browser-trump-board-v1.0.0.zip` を Release に添付
- `VERSION.txt` / `アプリを開く.bat` / `起動のしかた.txt` 追加
- **9タイトルのみ遊べる・1台モードのみ** と README に明記
- それ以外は `modesSoon` で「準備中」表示

---

# 2026-07-12（深夜〜未明）— ボードゲーム・○×・将棋・イト

## サイト名
- **Board & Card Online** → **ブラウザ・トランプ＆ボード** に改称（全ページ）

## 公開方針の切り替え
- 実装済みでも **1台で遊べる以外は「準備中」** に変更（V1確定に向けて）
- 準備中ゲームも **一覧には表示**（「遊べる」だけではなくカタログ全体を見せる）

## オセロ
- 1台の端末で2人交代プレイとして実装
- `Downloads\osero` のアイコンを採用
- アイコン差し替え（2回目の画像に更新）
- 石を置く **短いアニメーション** 追加
- アニメーションが二重にひっくり返る不具合を修正
- 人狼・オセロの「各自スマホ」「オンライン」を **準備中** に

## 五目並べ
- 1台2人で実装
- `igo.icon` のアイコンを反映

## ノーマル○×ゲーム
- 通常の三目並べとして実装（当初「3目」表記 → **ノーマル○×ゲーム** に改名）
- **instantLocal**: 「1台で遊ぶ」選択で即盤面へ（設定画面スキップ）
- ○×を置いたとき **マスの枠サイズが変わる** 不具合を CSS 修正（`aspect-ratio`）

## 消える○×
- 4個目を置くと最古の駒が消えるルールで実装
- instantLocal 対応
- マスサイズ固定の CSS 適用

## マトリョーシカ○×
- 小・中・大のコマを重ねる三目並べを実装
- 出番のプレイヤーは **手持ちの枠が緑** になるよう表示
- 大・中・小を **それぞれ3個ずつ** に変更
- 埋もれたコマの履歴表示（ドット）を **非表示** に（隠れている感を出す）
- instantLocal 対応

## 将棋
- 9×9 将棋を1台2人で実装
- 盤を **マスごとの格子状** に
- 駒を指定デザインの文字表示に
- 歩兵の初期位置を **3列目・7列目** に修正
- **持ち駒** 概念を追加（取った駒を打てる）
- **何手目か** を画面に表示
- 途中で止まる・ガイド線消失・持ち駒選択不可などのバグを複数回修正
- 先手の持ち駒エリアを **盤の下** に配置
- **王手** 時に一瞬「王手！」エフェクト
- **詰み** 時にゴールドの「詰み！」フラッシュ演出
- `shogi.icon` / `syogi.icon` のアイコンを差し替え

## ○×系アイコン
- `marubatu.icon` を **3つの○×ゲームすべて** に適用

## イト（Ito）
- クモノイト方式（協力・ライフ3）を実装
- お題を **ランダム** または **自由記述** で選べるように
- お題カテゴリ拡充:
  - 一般・学校系・社会人系・下ネタ系（当初「エロ系」→ **下ネタ系** に改名）
  - **ゲーム系・アニメ系** を追加
  - 各系統 **15個** のお題を用意

## ワードウルフ
- 最初の画面で **お題カテゴリ** を選択可能に
- **ウルフの人数** も選択可能に
- `wordwolf` フォルダのアイコンを反映
- 遊び方カードの枠を **白線** で強調

## デプロイ
- 上記を複数回 EC2 にデプロイ（「デプロイして」「デプロイ頼む」等）

---

# 2026-07-12（未明）— マルチデバイス・QR・HTTPS・プレイ回数

## 各自のスマホ（人狼）
- 「各自のスマホで遊ぶ」でルーム作成できない不具合を修正（`Sync.mode` 未設定）
- ルームコードを **画面中央に大きく** 表示
- ホストがロビーで **基本構成 / カスタム** を選択
- 参加者に構成を表示、**人狼を始める** でその構成のまま開始

## QRコード参加
- ルーム画面に **QRコード** 表示
- CDN の壊れたパスを修正 → `qrcodejs` を `js/vendor/` にローカル配置
- QRの下に **参加URLのテキストリンク** も表示（読めない場合の代替）
- 参加URLを `http://54.65.228.227/games/play.html?...` に固定
- nginx: `/play.html` → `/games/play.html` リダイレクト
- QRで「サイトにアクセスできなかった」問題を修正

## HTTPS（Brave対策）
- Brave で `http://54.65.228.227/` が開けない問題
- **自己署名HTTPS** を nginx に設定（追加コスト0円）
- 初回は「詳細設定→続行」が必要 → 画面に **Brave向け注意** を追加
- 独自ドメイン + Let's Encrypt が本番解決策と説明

## 累計プレイ回数
- ナビ「遊び方」の横に **○回プレイ** 表示を追加
- ゲーム開始時にサーバーで +1 集計
- 過去分として **+200** 加算
- トップを読み込んでも増えない不具合を修正（APIパス・nginxプロキシ）
- 201回にならない問題を修正
- **1台モードではルームコード不要** に（ローカル時はコードカード非表示）

## 人狼（追加）
- **妖狐** 役職を実装（襲撃不死・占いで死亡・単独勝利）
- 夜フェーズに **「役職の説明を見る」** ボタン
- 妖狐が占われて死んだ朝に **専用メッセージ** を表示

---

# 2026-07-11 夜〜深夜 — 人狼大改修

## ゲームフロー（1台回し）— ユーザー指定どおり再設計
- **役職確認 → 夜 → 朝 → 昼 → 投票** のループに統一
- 役職確認は **最初の1回だけ**
- 夜: 全員目を閉じる → 人狼 → 占い師 → 狩人 → その他役職
- 後に **プレイヤー1から順番** に全員回す方式へ変更（能力なしは「今夜の行動はありません」）
- 投票も **死亡者を除いて順番に回す**
- 昼の議論 **5分** タイマー
- 昼と投票画面を **分離**
- 最後の人が投票すると **自動集計** → 処刑結果 → **夜へ進む**
- 投票から夜に移行しないバグを修正
- 各フェーズタイトル下に **人数・構成を常時表示**（例: `6人 · 村人2・占い師・狩人・人狼・狂人`）

## 勝利条件
- **村人勝ち**: 人狼を全員追放
- **人狼勝ち**: 人狼の人数 ＝ それ以外の人数
- 妖狐単独勝利は後日追加（上記参照）

## 役職・構成
- 役職一覧を大幅拡充（村人・占い師・霊能者・狩人・共有者・猫又・人狼・狂人・狂信者・狼少年・妖狐・恋人 等）
- 5〜13人の **おすすめ構成プリセット**
- ロビーで **基本構成 / カスタム** 選択（後の各自スマホ対応で本格化）
- 占い師の行動順を **人狼の前後どちらでも** OK に
- 占い師の結果が表示されないバグを修正
- 最後の人が投票できないバグを修正

## 人狼 UI・カード
- 役職カード画像を `jinrou-cards` フォルダから反映
- 人狼専用 **背景画像**（`WerewolfBackground`）を複数回差し替え・調整
  - 狼と月が見える配置
  - 全面表示（`cover`）
  - オーダーメイド工房リンクを画像・フッターから削除
- 人狼アイコン（`Werewolf` フォルダ）をゲームタイルに反映
- 役職説明を **別ボタン** で表示（遊び方ページからではなくゲーム中から）
- タグライン「正体を隠し、仲間を信じ…」の文言変更
- 押せるボタンを **わかりやすく** スタイル改善

## 遊び方選択 UI
- 3択を以下の文言・デザインに:
  - **1台で遊ぶ** — 1台のスマホを順番に回す
  - **📱 各自のスマホで遊ぶ** ⭐⭐⭐⭐⭐ — ルームコードで参加
  - **🌐 オンラインで遊ぶ** — 離れた場所の友達と（準備中）
- 1台で遊ぶは「ルームを作る」ではなく **PLAY** 表記に
- 遊び方3択カードの **白枠** を強調

---

# 2026-07-11 夕方〜夜 — サイト構成・UI・ルーム

## ページ構成
- **guide.html** 新設 — ゲーム説明を別ページへ
- トップ・一覧から説明文削除、コンパクトタイル化
- **categories.html** 新設 — ジャンル別一覧
- カテゴリ名整理: パーティゲーム / カードゲーム / ボードゲーム / 協力ゲーム / サイコロ
- **play.html** 新設 — 遊び方3択をゲーム別ページへ
- トップは **ゲーム一覧のみ**（人狼以外も同様に `play.html?game=...` へ）
- 古い `index.html?game=werewolf` は `play.html` へリダイレクト
- ヘッダー下のタグライン（説明一行）を削除してシンプル化
- トランプカテゴリに `PlayingCards.icon` を反映

## UI刷新
- ダークテーマのモダンUI（`--bg`, `--surface`, `--accent` 等）
- ナビ: トップ / カテゴリ / 遊び方
- ゲームタイル: アイコン・名前・人数・「遊ぶ」のみ
- 「説明を見る」→ ガイドページへリンク

## ルーム作成フロー
- 「ルームを作る」押下後に **人数選択＋名前入力** 画面へ
- デフォルト名: プレイヤー1, プレイヤー2…（人数に応じて自動）
- **名前保存機能を削除**
- 名前例は「みずそ」のまま、ルームコード例は **11AA**
- ルームの **人数を選べる** ように
- ゲーム開始ボタンが出ないバグ修正（`getGameModule` 読み込み）
- 人狼選択時に大きな **「人狼を始める（N人）」** ボタン
- 2人目以降の名前が入力できないバグ修正（0.8秒ごとの全再描画が原因 → 差分更新のみに）

## バグ修正
- トップで「ルームを作る」→ トップに戻るループを修正
- UIがデプロイ後に古い版に戻る問題 → 再デプロイ + キャッシュ注意
- nginx 403 → `chown nginx:nginx` / `chmod 755`

## SEO
- タイトル・description・keywords を日本語で整備
- `robots.txt` / `sitemap.xml` 追加
- 検索ワード候補のドキュメント（人狼 無料 ブラウザ 等）

---

# 2026-07-11 午後 — ゲーム実装（多くはV2向けコード）

## コヨーテ
- `coyote.js` 新規 — 自分のカードは見えないブラフゲーム
- ローカルモードで手元プレイヤーのカードだけ隠す修正

## コードネーム
- `codenames.js` 新規 — 5×5、スパイマスター、赤9/青8/ベージュ7/黒1
- その場モード: 端末を回してキーカード確認
- オンライン: スパイマスターだけ色が見える

## ババ抜き
- `oldmaid.js` 新規 — ペア捨て、ジョーカーが残った人の負け
- オンライン手札同期の `secrets.js` 調整

## 七並べ
- `sevens.js` 新規 — ♦7から、スートごとに並べる

## ポーカー3種 + チップ
- `poker-utils.js` — 役判定・チップ・カードUI・初心者ガイド
- **テキサスホールデム** — 手札2+場札5、SB10/BB20
- **セブンカード・スタッド** — 最大7枚、場札なし
- **ファイブカード・ドロー** — 5枚配布+交換
- 全員1000チップスタート、ポット表示、役の強さ一覧

## レジストリ
- 神経衰弱・スピード・99・ページワン等を「準備中」として登録
- `games.html` 一覧ページ新設（404防止）

---

# 2026-07-11 — 公開・インフラ

## EC2デプロイ
- `party-games` を `54.65.228.227` に初回アップロード
- 配置先: `/usr/share/nginx/html/games/`
- 革ショップ（`/`）と **別URL** で運用
- 革ショップナビに「🎲 ボードゲーム」→ `/games/` リンク追加
- デプロイ手順を README に記載
- 権限設定手順を追加（403対策）

## サイト公開
- 公開URL: http://54.65.228.227/games/
- ローカル確認: `python -m http.server`（後に V1 ZIP 用 PS1 サーバーへ）

---

# 初期版（2026-07-11 以前）

- プロジェクト `party-games` 立ち上げ
- `game-registry.js` によるゲーム登録基盤
- **イト（Ito）** — 協力・数値当て（初期版）
- **人狼** — 基本実装
- **ワードウルフ**
- **大富豪・ダウト** — コードあり
- `room_server.py` — ルームAPI・プレイ回数API
- `sync.js` — ローカル / オンライン同期の骨格
- `js/games/_template.js` — 新ゲーム用テンプレート

---

# Version 1 で遊べる9タイトル（固定）

| # | ゲーム | モード |
|---|--------|--------|
| 1 | 人狼 | 1台 |
| 2 | ワードウルフ | 1台 |
| 3 | イト | 1台 |
| 4 | オセロ | 1台 |
| 5 | ノーマル○× | 1台（instantLocal） |
| 6 | マトリョーシカ○× | 1台（instantLocal） |
| 7 | 五目並べ | 1台 |
| 8 | 将棋 | 1台 |
| 9 | 消える○× | 1台（instantLocal） |

**保存:** https://github.com/mizuso2020/browser-trump-board/releases/tag/v1.0.0

---

# 変更ファイル索引（主要）

| 領域 | 主なファイル |
|------|-------------|
| ゲーム登録 | `js/game-registry.js` |
| ルーム | `js/room.js`, `room.html` |
| 遊び方選択 | `js/play.js`, `play.html` |
| 一覧 | `js/index.js`, `js/games-catalog.js`, `categories.html` |
| 人狼 | `js/games/werewolf.js` |
| 将棋 | `js/games/shogi.js` |
| オセロ | `js/games/reversi.js` |
| 五目 | `js/games/gomoku.js` |
| ○×3種 | `tic-tac-toe.js`, `vanishing-ttt.js`, `matryoshka-ttt.js` |
| イト | `js/games/ito.js` |
| ワードウルフ | `js/games/wordwolf.js` |
| スタイル | `css/style.css` |
| 起動 | `アプリを開く.bat`, `scripts/local-server.ps1` |
| 統計 | `js/stats.js`, `js/stats-boot.js`, `api/room_server.py` |
| QR | `js/room-qr.js`, `js/vendor/qrcode.min.js` |
| 設定 | `js/site-config.js` |
