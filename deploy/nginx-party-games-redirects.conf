# /games/ 配下に置いた静的ファイルへのショートカット
location = /play.html {
    return 302 /games/play.html$is_args$args;
}

location = /room.html {
    return 302 /games/room.html$is_args$args;
}

location = /games/js/stats-boot.js {
    add_header Cache-Control "no-store, no-cache, must-revalidate";
}
